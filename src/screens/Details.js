import { View, Text, Image } from 'react-native';
import styles from '../styles/index';

export default function Details() {
	return (
		<View style={styles.container}>
			<Text style={styles.foodBold}>Details Page</Text>
		</View>
	);
}
